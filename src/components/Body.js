import ResCardContainer from "./ResCardContainer";
import Search from "./Search";

const Body = () => (
  <div className="body">
    <Search />
    <ResCardContainer />
  </div>
);

export default Body;
